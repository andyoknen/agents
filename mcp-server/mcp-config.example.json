{
  "mcpServers": {
    "orchestrator-agents-mcp": {
      "command": "node",
      "args": [
        "${workspaceFolder}/agents/mcp-server/dist/index.js"
      ],
      "cwd": "${workspaceFolder}"
    }
  }
}